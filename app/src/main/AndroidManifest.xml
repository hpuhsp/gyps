<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.swallow.gyps">

    <application
        android:name="com.swallow.gyps.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gyps"
        android:usesCleartextTraffic="true">
        <activity
            android:name="com.swallow.gyps.msc.VoiceInputActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.swallow.gyps.msc.SignUpActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.swallow.gyps.main.MainActivity"
            android:launchMode="singleTask"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>